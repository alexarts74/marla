<div class="container">
  <div class="row align-items-stretch">
    <div class="col">
      <h2>Welcome bark! Log in</h2>

      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), data: {turbo: false}) do |f| %>
        <div class="form-outline">
          <%= f.input :email,
                      required: false,
                      autofocus: true,
                      input_html: { autocomplete: "email" } %>
          <%= f.input :password,
                      required: false,
                      input_html: { autocomplete: "current-password" } %>
          <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Log in", class: "btn-flat"%>
        </div>
      <% end %>

    <%= render "devise/shared/links", class: "color" %>
  </div>
    <div class="col-12 col-md-6">
    <div id="dog-collage">

    <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(http://www.gif-king.com/files/uSers/gif-king-09c8d9afe79a57c3b3eda951214034d8.gif);">
      <div class="container">
        <h1>the best collection of humans on the internet</h1>
      </div>
    </div>
    </div>
    </div>
    </div>
</div>

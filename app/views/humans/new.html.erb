<div class="container">
  <h2>Create your human</h2>

  <%= simple_form_for [@dog, @human] do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :price %>
    <%= f.association :categories, as: :radio_buttons %>
    <%= f.button :submit %>
  <% end %>
</div>
